#!/bin/sh

# alt rewrites the machine.lua symlink; hyprland auto-reloads during the gap,
# fails the require("machine") and drops every monitor rule to the generic one.
command -v hyprctl >/dev/null 2>&1 || exit 0
hyprctl reload >/dev/null 2>&1 || true
