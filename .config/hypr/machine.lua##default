-- Default machine config: used when no yadm class alternate applies.
-- Keeps hyprland.lua's require("machine") from dangling on unknown machines.
hl.monitor({ output = "", mode = "preferred", position = "auto", scale = 1 })
